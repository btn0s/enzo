# Enzo tracker

For every Enzo feeding, pee, poop, combined diaper, or tracker-status request, read [AUTOMATION.md](AUTOMATION.md) and use `bin/enzo`. Treat the CLI as the single write path so the tracker and `Feed Enzo` reminder stay synchronized.
