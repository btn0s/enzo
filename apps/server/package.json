{
  "name": "enzo-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 4318",
    "deploy": "wrangler d1 migrations apply enzo --remote && wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.128.0"
  }
}
